const { rob } = require("../plugins/rpg_plugin")

module.exports = {
    name:'rob',
    description:"rob someone's money",
    category:"economy",
    async execute(bot,msg,args,Discord){
        return
        
    }
}